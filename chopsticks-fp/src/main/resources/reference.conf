akka.coordinated-shutdown.phases {
  app-interruption {
    timeout = 60s
  }

  before-service-unbind {
    depends-on = [app-interruption]
  }
}

iz-logging {
  level = "DEBUG"
  level = ${?log.level}
  colored-output = true

  //  json-file-sink {
  //    path = "json-logs"
  //    rotation-max-file-count = 10
  //    rotation-max-file-bytes = 104857600
  //  }
}


# TODO Remove this once this issue is resolved https://github.com/zio/izumi-reflect/issues/98
izumi.reflect.rtti.optimized.equals = false
